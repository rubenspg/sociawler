require 'spec_helper'

describe Sociawler do
  it 'has a version number' do
    expect(Sociawler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
